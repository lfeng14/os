- 首先跟硬件交互是很复杂的，所以你看看驱动程序就知道了，种类繁多，数量庞大，质量最差。
  因为设备只有插上时才能被测试，如果设备永远没遇到就没法测试。
 <img width="1010" height="800" alt="image" src="https://github.com/user-attachments/assets/548b53d4-4227-4cd5-861e-6bb427a1907a" />
- I/O 设备的主要功能：输入和输出
  “能够读 (read) 写 (write) 的字节序列 (流或数组)”
  常见的设备都满足这个模型
  终端/串口 - 字节流
  打印机 - 字节流 (例如 PostScript 文件)
  硬盘 - 字节数组 (按块访问)
  GPU - 字节流 (控制) + 字节数组 (显存)
  操作系统：设备 = 支持各类操作的对象 (文件)

  read - 从设备某个指定的位置读出数据
  write - 向设备某个指定位置写入数据
  ioctl - 读取/设置设备的状态

- 驱动打印会输出到dmesg 
